<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.test">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="FAST"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.DangNhap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Search.search_thongtin" />
        <activity android:name=".Activity.thongtin_donhang" />
        <activity android:name=".Activity.GiaoHang" />
        <activity android:name=".Activity.ThongTinCaNhan" />
        <activity android:name=".Activity.DangKy" />
        <activity android:name=".Activity.QuenMatKhau" />
        <activity android:name=".Activity.DoiMatKhau" />
        <activity
            android:name=".Activity.ThongTinDatHang"
            android:label="Thông Tin Giao Hàng" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBPwqAw5hBx-BgaT56s9tqbsqYy4-81PAs" />

        <activity
            android:name=".Activity.ThongTinGiaoHang"
            android:label="Thông Tin Đặt Hàng"></activity>
        <activity android:name=".Activity.Nhanvien"></activity>
    </application>

</manifest>